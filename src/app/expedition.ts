export class Expedition {
    quatite: number;
    produit: string;
    description: string;
   
  
    constructor(quatite: number, produit: string, description: string, telephone: string, email: string) {
      this.quatite = quatite;
      this.produit = produit;
      this.description = description;
     
    }
}
